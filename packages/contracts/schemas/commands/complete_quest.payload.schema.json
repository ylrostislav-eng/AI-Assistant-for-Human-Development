{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.system.local/commands/complete_quest.payload.schema.json",
  "title": "CompleteQuestPayloadDraftV1",
  "description": "Target and expected version belong in the envelope. The duplicates here are legacy and accepted only when they agree with the envelope. The measured Activity fact (actual amount/duration/occurred time) is not part of this draft yet; a client MUST NOT send it here silently.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "occurrence_id": { "type": "string", "format": "uuid" },
    "expected_version": { "type": "integer", "minimum": 0 },
    "variant": { "enum": ["normal", "minimum"] }
  }
}
