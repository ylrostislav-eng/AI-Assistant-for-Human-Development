import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Файлы проверок выполняются по очереди: интеграционные проверки работают с
    // одной тестовой базой и меняют схему. При параллельном запуске один файл
    // удаляет таблицы, пока другой их читает, и падения выглядят как случайные.
    fileParallelism: false,
  },
});
